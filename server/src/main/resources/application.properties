# Base configuration for the MCP files server.
# Configure the working directory with:
# mcp.files.base-dir=/path/to/workspace

# Transport customization:
# webmvc or websocket
mcp.files.transport.type=webmvc
# mcp.files.transport.endpoint=/mcp
# mcp.files.transport.disallow-delete=false
# mcp.files.transport.keep-alive-interval=30s

# Basic authentication credentials for the HTTP transport.
# mcp.files.security.username=mcpdev
# mcp.files.security.password=change-me
